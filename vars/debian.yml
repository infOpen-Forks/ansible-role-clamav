---

# Default Debian variables

clamav_os_default_packages:
  - name: 'clamav'
    state: 'present'
  - name: 'clamav-base'
    state: 'present'
  - name: 'clamav-daemon'
    state: 'present'

clamav_os_default_service_name: 'clamav'
